<ul id="games">
  <% @games.each do |game| %>
    <li>
      <%= link_to game.away.name, game.away %> (<%= game.score_away %>)
      vs.
      <%= link_to game.home.name, game.home %> (<%= game.score_home %>)
    </li>
  <% end %>
</ul>

<%= link_to 'New Game', new_game_url %>